# Results

Final result exports will be added here only after the queries that generate them have been executed against the validated database. Planned outputs are:

- `data_scope_validation.csv`
- `geographic_coverage.csv`
- `monthly_posting_trends_by_role.csv`
- `role_specific_top_skills.csv`
- `role_profiles.csv`

They are not created as empty placeholder CSV files: every committed result must contain reproducible, validated output.
