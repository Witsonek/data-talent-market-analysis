# Power BI stage

This folder is reserved for the separate Power BI workflow. The report should connect to the validated PostgreSQL model or a documented extract, and its final screenshots should be stored in `power-bi/screenshots/`.
